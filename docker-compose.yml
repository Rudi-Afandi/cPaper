version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cpaper-frontend
    ports:
      - "4173:4173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
